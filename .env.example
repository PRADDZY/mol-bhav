# === LLM ===
OPENAI_API_KEY=sk-...

# === Database ===
MONGODB_URL=mongodb://molbhav:molbhav_secret@localhost:27017
MONGODB_DB_NAME=mol_bhav
MONGO_USERNAME=molbhav
MONGO_PASSWORD=molbhav_secret

# === Redis ===
REDIS_URL=redis://:redis_secret@localhost:6379/0
REDIS_PASSWORD=redis_secret

# === Negotiation Defaults ===
DEFAULT_BETA=5.0
DEFAULT_ALPHA=0.6
DEFAULT_MAX_ROUNDS=15
DEFAULT_SESSION_TTL_SECONDS=300

# === Security ===
MIN_RESPONSE_DELAY_MS=2000
CORS_ALLOWED_ORIGINS=["http://localhost:3000"]
API_ADMIN_KEY=          # leave blank for dev (no auth); set a strong key for production
ENV=development         # "development" or "production" (disables /docs, /redoc)
